import './style.scss'
const TitleForPages = ({title}) => {
   
    return (
        <div className='title_for_page'>
            <h1>{title}</h1>
        </div>
        
    );
};

export default TitleForPages;